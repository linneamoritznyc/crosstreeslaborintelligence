{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "services/matching-api/Dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/sh -c \"uvicorn src.main:app --host 0.0.0.0 --port $PORT\"",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
